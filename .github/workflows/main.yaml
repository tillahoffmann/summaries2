name: Summary Statistics
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v3
      - name: Setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: pip
      - name: Pre-install pybind11, fasttr, and torch.
        run: |
          pip install `grep pybind11== requirements.txt` `grep wheel== requirements.txt` `grep torch== requirements.txt`
          pip install --no-build-isolation `grep -oE 'git\+.*?fasttr.git@\w+' requirements.txt`
      - name: Install Python dependencies.
        run: pip install --extra-index-url https://download.pytorch.org/whl/cpu --no-compile --no-deps -r requirements.txt
      - name: Lint the code.
        run: flake8
      - name: Run the tests.
        run: pytest --cov=summaries --cov-report=term-missing --cov-fail-under=100
